* PLAYTEST 2026-08-16 (vwm + Ollama opponent)
** DONE Switching Ollama opponent after clocks start: handshake connect fails
   ITIMER_REAL / SIGALRM every 100ms interrupted connect() (EINTR). close()
   then clobbered errno so the dialog only said "connect host:port failed".
   Fixed on `fixes`: retry connect/read on EINTR; preserve errno; strerror.
** DONE Handshake timeout 15s is too short for 30B+ models
   Bumped OLLAMA_HANDSHAKE_TIMEOUT_SEC to 60 on `fixes`.
** DONE Connection picker: Enter is easy to miss from vterm send-keys
   construct_menu bound only '\n'. KEY_ENTER / '\r' (vwm --key Enter) did
   not activate. Fixed on `fixes`: bind '\\n', '\\r', and KEY_ENTER.

*** Still open — cboard (game)
** DONE Error dialog truncates long Ollama handshake messages
   Multi-line errors now use the scrollable message window instead of the
   one-line OK popup that flattened newlines and clipped at COLS.
** DONE After a failed handshake, mode becomes human/engine + Engine offline
   Restore CF_HUMAN so status is human/human after a failed reconnect.
** DONE Illegal engine move (e.g. Black plays e2e4) only pops Error + OK
   After dismiss, send go again to the Ollama bridge.
** DONE No in-UI hint that 'g' forces the engine to move after an error
   Invalid Ollama moves mention g in the error dialog.
** DONE Saved-connection list: easy to overshoot onto Disconnect / Remove
   Picker now opens on the first saved connection, not New connection.
** TODO Side clocks stay 00:00:00 while Total ticks
   After 1.e4 1...e5 2.Nf3 2...Nc6 (~4.5 min), White and Black both still
   read 00:00:00. Only Total advances. Side clocks should run for the
   player to move (and persist after the move).
** TODO PGN Black tag stays "?" after Connect Ollama
   White is filled (local user). Black never becomes the model / connection
   name (qwen3.6:35b-a3b). Save/export then looks like an unrated human game.
** TODO No auto-reconnect to last saved Ollama opponent
   Every launch is human/human until File → Connect Ollama. Remember last
   used connection or offer it on startup.

*** Still open — vwm
** TODO F10 menubar: vwm-msg --key F10 does not deliver KEY_F(10)
   CSI \e[21~ opened the bar; --key F10 did not. Check vterm_write_pipe
   F10 mapping vs VTerm HiColor terminfo kf10.

* BUG Don't rotate the board when leaving history mode.
  When loading a PGN file. This also affects GF_BLACK_OPENING. There
  is commented code that will allow a human to open as black but in
  gnuchess the 'white' and 'black' commands are not yet implemented
  (version 6.x anyway; v5.x does have these commands).

* BUG Speed up the move validator.
  It's alot slower since 0.1.6. I need suggestions on this.

* BUG An annotation without any move text is broken.
  For example:

    { game forfeited } 1-0

  with or without game tags. Not sure how to handle this. Maybe allocate
  a new history item for the empty board.

* BUG The 'draw' game result needs better handling.
  Parsing works and the 50 move draw rule works, but offering and
  accepting draws isn't implemented. Also, chess engine game results
  need (better) handling.

* BUG Solaris/Sun SIGALRM bugs and engine command window broken.
* BUG NetBSD has a crappy ncurses installtion. doing manual symlinks works
   Broken engine IO on PTY's. may be kernel related although screen works.


* FICS/ICS network game support.

* Email correspondence games.

* RAV move text support.
  Can parse into the move history and also save (buggy) but still need
  a (working) interface to it. Also add the RAV depth next to the
  piece on the board and fix the move history tree to show the RAV
  tree.

* Sort saved games by tag values specified by the PGN/EPD standard.

* Better matching of tag expressions when searching for games.
  Maybe a comma separated list of tag/value expressions.

* Match move text expressions across games.

* GPM mouse support.

* EPD game format.

* Make libchess its own project.
  This should handle everything not GUI including file parsing, chess
  engine and chess server interaction.

* Implement UCI (Universal Chess Interface).

